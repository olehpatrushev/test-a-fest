**Quick Installation Guide** *(for Ubuntu users)*
- Download project
- Configure .env file in project's root *(see .env.example file)*
- Run `composer install`
- Run `npm install`
- Run `php yii serve` to start development server 
  *(by default on http://localhost:8080)*